#include<stdio.h>

struct node{
	int data;
	struct node * link;
};

void main(){
	struct node *front, *rear;
	p = malloc(sizeof(struct node));
	q = malloc(sizeof(struct node));
	p->data=1;
	q->data=2;
	*front = p;
	*rear = q;
	
	rear

}